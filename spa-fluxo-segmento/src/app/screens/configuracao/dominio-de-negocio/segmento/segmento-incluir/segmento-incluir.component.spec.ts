import { SegmentoIncluirComponent } from './segmento-incluir.component';

describe('SegmentoIncluirComponent', () => {
  it('should create', () => {
    const comp = new SegmentoIncluirComponent();
    expect(comp).toBeTruthy();
  });
});
